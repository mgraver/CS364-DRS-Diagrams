@startuml

skinparam classAttributeIconSize 0

class Day {
  +hours : KeyValue<int, Hour>
  +name : int
  +getHour(int) : Hour
  +addHour(Hour) : void
  +updateHour(Hour) : void
  +deleteHour(Hour) : void
}

@enduml
